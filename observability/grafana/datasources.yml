apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # In Docker Compose, 'prometheus' resolves to the container
    url: http://prometheus:9090 
    isDefault: true
    editable: false